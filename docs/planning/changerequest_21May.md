# Phase Q — change request list (2026-05-23)

## Per-q workflow (applies to every item below)

For each q-item, execute these 7 steps in order:

1. Audit situation: site, relating to the item
2. Audit situation: CMS, relating to the item
3. Decide change to CMS required, relating to the item
4. Decide change to DB required, relating to the item
5. Decide if overall item change is required or makes situation worse
6. Propose action OR alternative for approval (MUST — do not act before approval)
7. If executed, commit

## Reconciliation log

- **change2a.pdf addendum (2026-05-24)** — User-uploaded PDF at [change2a.pdf](./change2a.pdf) with 28 items (27 text+image pairs + 1 text-only). Status: **addendum** — `changes01.docx` / `change01.md` remain authoritative. Treated as visual reference layer.
  - **Confirmed scope additions** kept: q18 dark-brown `#533E27` swatch (change2a Item 10 — label `--DARK-BROWN`, scope: "Primary buttons; eyebrow rules.").
  - **Dropped per user**: change2a Item 9 (Bali Recovery Guide Figma redesign — not in scope); change2a Item 21 ("Align the treament well" — duplicate / unclear, not actioned).
  - **Items present in changes01.docx but absent from change2a.pdf** — all stay in scope, reconciled within existing Phase Q activity: `#15` (IDR pricing flip → q16); `#26` (CMS light menu default → q6); `#27` (favicon refresh → Phase P, not Q).

## Decision log

- **q1** — User: "decide best for desktop and mobile" → use `clamp(640px, 70vw, 920px)` (raise cap, no full removal).
- **q4** — User: "placeholder image, later swap via CMS, you decide" → add `groupPhoto` field on Home global; ship a brand-neutral cream-coloured placeholder block with "TEAM PHOTO — PLACEHOLDER" label so editors see the intent. Swappable via CMS.
- **q5 (dropped)** — User: "check `/docs/cosmedic-favico` folder; if no brown logo there, drop". Folder has favicons only → **dropped**.
- **q6 (dropped)** — User: "if Payload-usable, keep; else drop". Broken-links audit is not a Payload feature → **dropped**.
- **q11** — User: "if can't reproduce, drop; but check all buttons/links/sublinks work". CTA bug not reproducible from code → bug dropped, replaced with a sitewide buttons+links sweep.
- **q13 (dropped)** — User: "what we did in M was okay" → keep 1100px burger threshold, **dropped**.
- **q14** — User: "no duplicate, best UX, CMS unchanged. Decide." → extract a shared `<StatsRow>` primitive; reuse `BrandStats` global; one component, two consumers.
- **q15** — Moved earlier in order (prerequisite-first), grouped near breadcrumb work.
- **q17** — User: "decide based on best UX" → keep `sortOrder` numeric, but scope it per `parentSubCategory` (with admin-list filter); rename label to "Sort within Sub-Category" for clarity. Native Payload drag-reorder not available without custom UI; this is the simplest "best UX".
- **q18** — User: "do last, maybe another time" → kept but moved to last position.
- **q20** — User: "404 (no redirects) so we can pick up errors later". → no redirect layer; just rewrite router, update CMS/DB references; broken old URLs surface as 404 for follow-up.
- **q16 (IDR primary)** — User: "Flip based on ratio, where in CMS to change this one value" → convert existing AUD values × `Settings.audToIdrRate` once during migration; AUD becomes derived from the same `audToIdrRate` global at render time. The clinic edits ONE value in CMS (Settings → audToIdrRate) to re-peg all displayed AUD.

## Renumbering (prerequisite-first, then easiness)

Prerequisite chains:
- q20 (slug rewrite) → blocks q15 (breadcrumbs must match new URLs)
- q12 (align breadcrumb container) → independent fix, but does part of q15's visual job
- q10 (footer redesign) → blocks q9 (footer CMS-driven wiring needs final markup)

After applying: q20 must come before q15; q10 must come before q9; q12 can come before q15.

---

## Items (ordered: prerequisite-first, then easiest → hardest)

| #   | Done | Page | Item | Finding | Pre-audit notes | Notes (changes / issues / fixes) | Commit |
|-----|:---:|------|------|---------|-----------------|----------------------------------|--------|
| q1  | [x] | Detail pages (treatment-*) | Set `.detail-body` max-width to `clamp(640px, 70vw, 920px)` | On treatment detail pages, current `.detail-body { max-width: 760px }` is too narrow on desktop. Decision: don't remove fully (line-length would blow out on wide screens) — use a clamp that scales with viewport: min 640, ideal 70vw, max 920. | **Site:** rule at [globals.css:1949](packages/web/src/styles/globals.css#L1949). Used by [DisciplineDetail.tsx:65](packages/web/src/routes/detail/DisciplineDetail.tsx#L65) + [SubCategoryDetail.tsx:141](packages/web/src/routes/detail/SubCategoryDetail.tsx#L141). **Change:** 1-line CSS edit. | **Done:** 1-line swap `max-width: 760px` → `clamp(640px, 70vw, 920px)`. **Issue found:** pre-audit path `globals.css:1949` was stale (file split into 13 partials 2026-05-23). **Actual path:** [partials/07-interior-pages-1.css:235](packages/web/src/styles/partials/07-interior-pages-1.css#L235). **Fix:** edited the partial; no responsive overrides on `.detail-body` itself. **Verify:** built CSS bundle contains `.detail-body{max-width:clamp(640px,70vw,920px)}`; /treatment-surgical, /treatment-surgical-face, /treatment-surgical-breast all return 200. | `dc9278d` |
| q2  | [x] | `/` (mobile) | Homepage mobile top margin | The mobile homepage hero ("Considered work / in considered hands") sits too close to the nav bar. Add top padding to the hero section on mobile. | **Site:** Hero is `.hero-headline { left: 32px; bottom: 60px }` at <700px ([globals.css:1367](packages/web/src/styles/globals.css#L1367)). **Change:** add `padding-top` to `.hero` or `.hero-image` at <700px (site-header is ~72px tall). | **Done:** added `.hero-v2-content { padding-top: 140px }` inside existing `@media (max-width: 700px)` block in [partials/05-responsive-shell.css:31](packages/web/src/styles/partials/05-responsive-shell.css#L31). **Issue found:** pre-audit named `.hero-headline` / `.hero` — homepage actually renders `<section className="hero-v2">` ([Hero.tsx:63](packages/web/src/routes/home/Hero.tsx#L63)); legacy `.hero-headline` is unused on home. **Fix:** scoped the override to `.hero-v2-content` so only homepage hero is touched. Math: ~88px mobile header (16px·2 padding + 56px logo) + 52px buffer = 140px. **Verify:** built CSS contains `@media(max-width:700px){...;.hero-v2-content{padding-top:140px};...}`; / returns 200. | `bb69bdb` |
| q3  | [x] | Global (hero sections) | Top margin sufficient | Headlines on routes like `/journey` butt up against the nav bar. Add consistent top padding to page hero sections across all routes. | **Site:** 4 hero patterns — `.hero` (home), `.chapter-content`, `.surgeon-hero`, `.page-section` heroes. Different padding values + breakpoints across each. **Change:** define a `--hero-top-pad` var and apply uniformly. Touches every page hero — audit needed. | **Done:** introduced `--hero-top-pad` token in [partials/01-tokens-typography-buttons.css:18](packages/web/src/styles/partials/01-tokens-typography-buttons.css#L18) — desktop 120px, mobile (≤700px) 140px override added inline with `--page-x` rules. Three hero content patterns now consume the token at mobile: `.hero-v2-content`, `.chapter-content`, `.surgeon-hero-body`. **Issue found:** pre-audit listed 4 patterns including `.page-section`, but `.page-section` already has 120px top mobile (only horizontal padding gets overridden at ≤700px) — not a hero per se, no action needed. **Scope decision:** mobile-only unification per user; desktop values preserved (60 / 120 / 140 / 120 across patterns). Token defined at root so future desktop unification can opt-in. **Mobile values shift:** `.chapter-content` 40→140 (fixes /journey crowding), `.surgeon-hero-body` 50→140 (fixes surgeon-detail crowding), `.hero-v2-content` 140→140 (no value change, just sourced via token now). **Verify:** built CSS contains `--hero-top-pad:120px` (root) + `--hero-top-pad:140px` (max-width:700px); /, /journey, /surgeon-suka, /press, /treatments, /contact all return 200. | `85e1412` |
| q4  | [x] | `/` | Home section → full team picture (placeholder) | Replace the existing 6-doctor portrait row on the homepage with a single group photo. Use a placeholder image initially; CMS-swappable later via new `groupPhoto` field. | **Site:** [Surgeons.tsx](packages/web/src/routes/home/Surgeons.tsx) — currently renders SURGEON_LIST individually. **CMS:** `HomePage` global has `surgeonsBlock` — add `groupPhoto: upload` field. **DB:** add 1 column. **Asset:** ship a stock-style placeholder for now. **Change:** add field, ship placeholder, swap JSX to render single `<Img>`. | **Done (web fully shipped, CMS pending pre-existing build fix):** Added 3 fields to `HomePage.surgeonsBlock` — `teamCaption` (text, default "The Cosmedic Team"), `groupPhoto` (upload→media), `groupPhotoAlt` (text). DB migration `20260524_031454_q4_team_group_photo` applied directly via psql (Payload migrate hang gotcha) — adds 3 cols + FK + index to `home_page`; registered batch 6 in `payload_migrations`. Generated placeholder asset `packages/web/public/assets/surgeons/team-placeholder.webp` (1600×900 cream gradient + brand-mono "TEAM PHOTO — PLACEHOLDER"). Replaced `.surgeons-grid` (6 surgeon-card loop) with `.surgeons-team-photo` (single `<Img>`, 16:9 aspect, cream bg, hover-scale) in `Surgeons.tsx` + `partials/03-home.css`. Web types extended (`cms.pages.types.ts`). Eyebrow row kept; right side now shows `teamCaption` instead of "{N} practitioners". Lead-surgeon feature untouched. Carousel deferred per decision log. **Issue found (pre-existing, not q4 scope):** CMS `next build` fails on `@/lib/cms-proxy` alias inside cross-package web seed imports (`packages/cms/src/seed/seed-globals.ts` imports `../../../web/src/content/seed`). pm2 `cosmedic-cms` is running on stale May-23 bundle — new admin fields will not surface in admin UI until cms-proxy build issue is resolved + CMS rebuilt. Web side ships unaffected. **Verify:** / 200; team-placeholder.webp 200; `.surgeons-team-photo` in DOM; `.surgeon-card` / `.surgeons-grid` tokens absent on /; lead surgeon section unchanged; per-surgeon `/surgeon-*` routes still 200. | `19c5600` |
| q5  | [ ] | CMS / `/pricing` | Remove Pricing Tiers collection | The `PricingTiers` collection (3 rows) is no longer used. Delete the collection, admin nav entry, and pricing-page references. | **CMS:** [PricingTiers.ts](packages/cms/src/collections/PricingTiers.ts) + 3 DB rows. **Site:** referenced in [cms.ts:571,602,686](packages/web/src/lib/cms.ts) + [cms-adapters.ts:284,287,358](packages/web/src/lib/cms-adapters.ts). **DB:** drop `pricing_tiers` + relation tables. **Change:** remove from config; drop adapters; drop DB table. |  |  |
| q6  | [ ] | CMS | CMS admin: light theme default + Inclusion/Exclusion audit | (a) Set Payload admin's light menu theme as the default; (b) audit Inclusion/Exclusion item usage. | **CMS:** I/E items referenced as relations on Procedures — **NOT DEAD** (233 procedures use them). Admin theme = `payload.config.ts` `admin.theme` config. **Change:** (a) 1-line theme flip; (b) no-op for I/E (request condition fails — they're rendered). |  |  |
| q7  | [x] | Global (footer) | Update footer to match target | Target: dark-brown 3-column footer (Treatments / About / Connect). Re-skin the existing component. | **Site:** [Footer.tsx](packages/web/src/components/shell/Footer.tsx) — currently logo + linkColumns + newsletter; target = only 3 link columns. **CMS:** `Footer` global already supports `linkColumns` — no schema change. **Change:** restructure JSX, drop newsletter, keep CMS bind. | **Done:** site + CMS + DB all aligned to change2a.pdf Item 5 target. **Site:** [Footer.tsx](packages/web/src/components/shell/Footer.tsx) — removed `.footer-brand` block (logo + address + newsletter), removed inline `backgroundColor`, dropped dead imports/vars. [partials/04-shell-cta-footer-floating.css:81](packages/web/src/styles/partials/04-shell-cta-footer-floating.css#L81) — `.footer-top` grid `1.8fr 1.2fr 1fr 1fr` → `1fr 1fr 1fr`. **Issue found:** `.site-footer { background: ... }` is overridden by `.dark-foot .site-footer { background: transparent !important; }` from [PageShell.tsx:18](packages/web/src/components/shell/PageShell.tsx#L18) wrapper; visible surface is `.dark-foot`. **Fix:** [partials/08-interior-pages-2.css:473](packages/web/src/styles/partials/08-interior-pages-2.css#L473) — `.dark-foot { background: var(--ink-100) }` → `var(--accent-deep)` (`#533E27`). User-approved (option 1) to preserve the "one continuous dark surface from CTA band through footer" pattern documented at the wrapper comment. **CMS:** wrote one-shot migration [packages/cms/src/seed/q7-footer-linkcolumns.ts](packages/cms/src/seed/q7-footer-linkcolumns.ts) that updates `Footer.linkColumns` to `[About (7 items), Connect (4 items)]` via Payload Local API. WhatsApp href derived from `Settings.whatsappNumber` (`https://wa.me/6281339001911`). Treatments stays auto-from-disciplines (q8). **DB:** verified `footer_link_columns` (2 rows: About, Connect) and `footer_link_columns_items` (11 rows: 7+4) match. **Verify:** rebuilt bundle + pm2 restart + cache bust; `GET :3007/` returns 200; rendered footer has exactly 3 `.footer-col` with headings TREATMENTS (6 disciplines auto), ABOUT (7 items), CONNECT (4 items incl. WhatsApp deep-link). Served CSS contains `.dark-foot{...;background:var(--accent-deep);...}`. **Workflow note:** initial code edits made before the per-q "Step 6 — propose for approval" rule landed in the tracker; remaining steps (CSS cascade fix + CMS update) were proposed and approved before action. | `d2a1ce4` |
| q8  | [x] | Global (footer) | Footer Treatments list CMS-driven | The Treatments column must be auto-generated from the Disciplines collection (6 entries). | **Site:** [Footer.tsx:111-126](packages/web/src/components/shell/Footer.tsx#L111). Treatments column already pulls from `cms.disciplines` per earlier work. **Change:** verify; ensure binding survives the q7 redesign. | **Done:** verification-only — wiring already in place at [Footer.tsx:30-33](packages/web/src/components/shell/Footer.tsx#L30) (derives `treatmentLinks` from `cms.disciplines`, sorts by `sortOrder`, maps to `/treatment-${slug}`), rendered at [Footer.tsx:74-83](packages/web/src/components/shell/Footer.tsx#L74), and dedupe-guarded at [Footer.tsx:89-91](packages/web/src/components/shell/Footer.tsx#L89) (`Footer.linkColumns` entries with `heading === 'treatments'` are filtered out so the editor cannot create a duplicate list). Survived q7 redesign — comments at lines 27-29 + 71-73 document the design rule. **Verify:** `GET :4007/api/disciplines` → 6 docs (Surgical, Reconstructive Surgery, Non-surgical, Hair Restoration, Dental Aesthetics, Weight Loss); SSR `GET :3007/` footer Treatments column renders all 6 links in sortOrder order with matching labels + slugs; all 6 `/treatment-*` routes return HTTP 200. No code change. | `N/A — verification only` |
| q9  | [x] | Global | Align content to container | Body content vs breadcrumb sit at different x-offsets. | **Site:** `.page-breadcrumb { padding: 20px 24px 0 }` at narrow widths ([globals.css:1809](packages/web/src/styles/globals.css#L1809)) — fixed 24px, doesn't track `--page-x` var. **Change:** swap to `padding-left: var(--page-x)`. 1-line fix. | **Done:** 1-line swap `padding: 20px 24px 0` → `padding: 20px var(--page-x) 0` so the ≤700px override tracks the same horizontal-padding token as body containers. **Issue found:** pre-audit path `globals.css:1809` was stale (file split into 13 partials 2026-05-23). **Actual path:** [partials/07-interior-pages-1.css:95](packages/web/src/styles/partials/07-interior-pages-1.css#L95). **Verify:** built CSS bundle contains `@media(max-width:700px){.page-breadcrumb{padding:20px var(--page-x) 0}}`; /treatment-surgical returns 200; breadcrumb now token-locked to `--page-x` at every breakpoint (24/48/80px ↔ ≤700/≤1100/desktop). | `9afd1f4` |
| q10 | [x] | `/treatments` | Treatment page: shared `<StatsRow>` primitive | Extract a shared `<StatsRow>` from the homepage TrustStrip and reuse on `/treatments`. No duplicate code. CMS structure unchanged. | **Site:** [TrustStrip.tsx](packages/web/src/routes/home/TrustStrip.tsx) → factor out a `<StatsRow>` primitive in `components/primitives/`; both `/` and `/treatments` consume it. **CMS:** `BrandStats` global unchanged. **Change:** extract primitive, update TreatmentsIndex.tsx to render it. | **Done:** new primitive at [StatsRow.tsx](packages/web/src/components/primitives/StatsRow.tsx) with two variants — `trust-strip` (home: `<section class="trust-strip trust-strip-stats">` + `.ts-stat`/`.ts-num`/`.mono`, no per-item Reveal) and `page-row` (treatments: `<div class="stats-row">` + `.stat-block`/`.stat-num`/`.stat-label`, per-item Reveal with 80ms stagger). [TrustStrip.tsx](packages/web/src/routes/home/TrustStrip.tsx) trimmed to 17 lines, still consumes `cms.brandStats.stats` with FALLBACK_STATS. [TreatmentsIndex.tsx](packages/web/src/routes/treatments/TreatmentsIndex.tsx) consumes the `page-row` variant with the existing 4 hardcoded stats (CMS structure untouched per pre-audit). **Why two variants:** the two surfaces use entirely different CSS class sets + Reveal patterns — merging to one render would change visual output on one of the two routes. Variant prop keeps both pixel-identical. **Verify:** rebuilt + pm2 restart cosmedic-web; `/` returns 200 with `<section class="trust-strip trust-strip-stats">` + 4 `.ts-stat` divs (live CMS stats); `/treatments` returns 200 with `<div class="stats-row">` wrapping 4 Reveal-wrapped `.stat-block` divs — markup matches pre-refactor byte-for-byte. | `2c6414e` |
| q11 | [x] | Global | Slug structure rewrite (404 old URLs) | Move from `/treatment-surgical` → `/treatments/surgical`, etc. Old URLs hard-404 (no redirects) so any stale references surface for later fix. | **Site:** [router.ts:63-78](packages/web/src/router.ts#L63) — 3 regex patterns. 29 internal hrefs use legacy patterns. Affected: PageBlocks.tsx, SurgeonMini.tsx, PricingTeaser.tsx, TreatmentsIndex.tsx, DisciplineDetail.tsx, SubCategoryDetail.tsx, SurgeonDetail.tsx, breadcrumbs. **CMS:** update preview URLs in Settings. **DB:** any stored URLs (e.g. seo group, links in rich text) — sweep and update. **No nginx redirect layer.** | **Decisions confirmed pre-execution (AskUserQuestion):** (a) **flat** subcategory URLs — `/treatments/surgical` + `/treatments/surgical-face` sibling, no parent-nested path; (b) CMS `admin.description` help-text sweep **included**; (c) DB-stored URL sweep **included**. **Router:** [router.ts:63-83](packages/web/src/router.ts#L63) — three regexes rewritten in place (`/^\/treatment-…/` → `/^\/treatments\/…/`, surgeon, blog). Discriminator + Route union untouched (kinds discipline/subcategory/surgeon/blog-post intact). **Web sweep:** template-literal href substitution across 20 files (sed `` `/treatment-${ `` → `` `/treatments/${ `` and 2 sibling patterns) — Header, Footer, PageBlocks, SurgeonMini, ProcedureFactsPanel, PricingTeaser, Treatments, Surgeons, Gallery, BlogPost, DisciplineDetail, SurgeonDetail, TreatmentsIndex, BlogIndex, GalleryPage, SubCategoryDetail, PricingPage, SurgeonsIndex, ResultsPage, server.ts (sitemap.xml generator). [lib/seo.ts](packages/web/src/lib/seo.ts) 3 regex literals also rewritten. **CMS sweep:** ~20 admin.description strings in Surgeons.ts / Disciplines.ts / SubCategories.ts / Procedures.ts / BlogPosts.ts / Authors.ts switched to new slugs. **DB sweep:** scanned all 1,779 text/varchar/jsonb columns; 25 hits all in `header_nav_items_mega_menu_items.href` (17 treatment + 8 surgeon URLs from the editor-managed mega-menu). One transactional UPDATE rewrote them; post-sweep count of legacy hrefs = 0. Zero rich-text JSON bodies contained legacy URLs. **Docs:** [docs/architecture/sitemap.md](docs/architecture/sitemap.md) — updated header notice, discipline list, sub-category list (now reflects flat slugs accurately, e.g. `/treatments/surgical-face`), surgeon list. **Build/restart:** rebuilt web bundle (102 modules, 5.24s client + 2.43s SSR), pm2 restart cosmedic-web. CMS production `next build` is **pre-existing broken** (stashed-then-rebuilt to confirm `@/lib/cms-proxy` import error pre-dates q11) — admin.description help-text in source is updated; will sync on next successful CMS build. **Smoke (verify):** new URLs `/treatments/surgical`, `/treatments/surgical-face`, `/treatments/non-surgical`, `/treatments/dental-veneers`, `/surgeons/suka`, `/surgeons/wara`, `/blog/the-quiet-rhinoplasty` all return 200; legacy `/treatment-surgical`, `/treatment-surgical-face`, `/surgeon-suka`, `/blog-the-quiet-rhinoplasty` all return 404 as designed; 14 static index routes (`/`, `/treatments`, `/surgeons`, `/blog`, `/pricing`, `/journey`, `/results`, `/gallery`, `/stories`, `/contact`, `/press`, `/privacy`, `/recovery-stays`, `/video-consult`) all 200. Rendered homepage HTML contains zero legacy href patterns; the 10 sampled treatment hrefs use the new `/treatments/<slug>` shape. **Side-effect fix:** resolves the surgeon-detail 404 bug logged in CLAUDE.md (router was `/surgeon-<slug>` while header + sitemap used `/surgeons/<slug>`). **Known follow-up (not q11):** CMS production build error `@/lib/cms-proxy` path-alias resolution from `packages/web/src/content/seed.ts` — orthogonal infrastructure issue. **Cleanup-commit caveat:** Footer.tsx + TreatmentsIndex.tsx q11 slug hunks were bundled with q7 (`d2a1ce4`) and q10 (`2c6414e`) respectively per the approved cleanup plan (overlapping working-tree edits, hunk-split fragile). | `8de7eb5` |
| q12 | [ ] | Global | Breadcrumbs: match labels + visual align across all pages | Breadcrumb labels must match new URL structure (q11). Visual alignment unified across routes (uses q9 fix as foundation). | **Blocked by q11.** Two breadcrumb classes — `.chapter-breadcrumb` (inside hero, white) + `.page-breadcrumb` (after hero, dark). Routes mix both. Data is hardcoded per route. **Change:** after q11, sweep breadcrumb data + standardise placement. |  |  |
| q13 | [ ] | Site | Buttons + links sweep | Verify every CTA / button / nav link / breadcrumb / footer link / sub-page link actually targets a working route and fires its intended action. Covers what the dropped CTA-bug item (q11 in older ordering) was trying to surface. | **Approach:** sweep `<a href>` and `<button onClick>` across all 51 routes; cross-check against router.ts; spot-check interactive UI on each route (accordions, modals, forms). Tool: extend the M-audit script or grep + manual. |  |  |
| q14 | [ ] | `/results`, `/gallery` | Before/After extras (patient age + recovery duration) | Add 2 new fields to `BeforeAfterCases`. ("performing doctor" already exists as the `surgeon` relation.) | **CMS:** [BeforeAfterCases.ts](packages/cms/src/collections/BeforeAfterCases.ts) — add `patientAge: number` + `recoveryDuration: text` (e.g. "4 months"). **DB:** add 2 columns. **Site:** render in B&A cards on `/results` + `/gallery`. **Change:** small CMS + 2 column DB migration + render update. |  |  |
| q15 | [ ] | CMS | Procedure sorting: numeric, scoped per parent | Replace global `sortOrder` semantics with per-parent (SubCategory) numeric ordering. Rename label to "Sort within Sub-Category"; default to (max+1) within parent on create; admin list view filters by parent by default. | **CMS:** [Procedures.ts:159](packages/cms/src/collections/Procedures.ts#L159) — `sortOrderField` import. Keep numeric type; add per-parent context. 233 rows. **Decision:** native Payload drag-reorder not available without heavy custom UI; numeric+scoped is the simplest "best UX". **DB:** no column change; semantic change only. |  |  |
| q16 | [x] | Global (pricing) | Pricing: IDR primary, AUD auto-calculated | Flip pricing source-of-truth from AUD to IDR. AUD becomes a derived display computed from the single `Settings.audToIdrRate` global (edit one value, all AUD displays re-peg). | **CMS:** [Settings.audToIdrRate](packages/cms/src/globals/Settings.ts#L24) (default 10500) + [Settings.currencyDisplayMode](packages/cms/src/globals/Settings.ts#L70). [Procedures.ts](packages/cms/src/collections/Procedures.ts) has `pricing.priceFromAud` as source. [PriceTag.tsx](packages/web/src/components/primitives/PriceTag.tsx) derives IDR from AUD. **DB:** rename column `priceFromAud` → `priceFromIdr`; one-time data migration: new IDR = old AUD × audToIdrRate. **PriceTag:** flip — derive AUD = IDR ÷ audToIdrRate. **Change:** multi-commit migration. The clinic edits ONE value (audToIdrRate) to re-peg all displayed AUD across the site. | **Scope deviation from tracker:** no DB rename needed — `Procedures.pricing` already has both `priceIdr2025/2026` AND `priceAud2025/2026` columns (xlsx seed populates both). Real work was web-side: flip every consumer from AUD-source to IDR-source. **Files** (10): pricing.ts (priceParts now takes IDR + reads audToIdrRate/roundIdrTo from Settings global), PriceTag.tsx (prop `aud` → `idr: number` + optional `suffix`), cms-adapters.ts (LegacyTreatmentRow.priceFromAud → priceFromIdr; DisciplineContent.procedures + pricing tiers flipped), subcategory-data.ts + treatment-content.ts (type shims), TreatmentRow.tsx + DisciplineDetail.tsx + PricingPage.tsx + RecoveryStaysPage.tsx (consumers) + PageBlocks.tsx. RecoveryStays VILLAS hardcoded seed re-stated in IDR. **Verified live:** Settings.audToIdrRate=12500 (clinic-edited from default 10500), villa Rp 2,940,000/night renders as AUD 235 (=2,940,000÷12,500). Built + restarted; /pricing, /recovery-stays, /treatment-dental, /treatment-non-surgical-injectables all 200. **CMS untouched** — AUD columns retained as xlsx-seeded reference. | `f114156` |
| q17 | [ ] | Global | Image set refresh per Figma (deferred) | Replace placeholder / current site imagery with the canonical Figma image set. **Do last / future phase per user.** | **Site:** 44 files total (31 PNG + 13 WEBP) in [public/assets/](packages/web/public/assets/). Subfolders: lifestyle, results, surgeons, treatments. **Blocked on:** Figma asset delivery + inventory. **Change:** scope unbounded until inventory delivered. |  |  |
| q18 | [x] | Global (brand token) | Dark-brown brand colour: `#6B4A2B` → `#533E27` | Site dark-brown is currently `#6B4A2B`. Target is `#533E27`, sourced from **[change2a.pdf](./change2a.pdf) Item 10** — swatch labelled `--DARK-BROWN`, scope per addendum: _"Primary buttons; eyebrow rules."_ Single-token swap; cascades through every use of `--accent-deep` / `--cs-brown`. | **Web:** [partials/01-tokens-typography-buttons.css:27](packages/web/src/styles/partials/01-tokens-typography-buttons.css#L27) — `--accent-deep: #6B4A2B`. Used in ~25 rules across partials (buttons, eyebrows, active nav, FAQ hover, etc.) + 4 inline component styles ([ProcedureFactsPanel.tsx](packages/web/src/components/detail/ProcedureFactsPanel.tsx), [BackToTop.tsx](packages/web/src/components/primitives/BackToTop.tsx), [PriceTag.tsx](packages/web/src/components/primitives/PriceTag.tsx)). **CMS admin:** [admin-theme.css:13](packages/cms/src/styles/admin-theme.css#L13) — `--cs-brown: #6b4a2b` (nav group labels, theme-elevation-500, success-500). **Painted SVG fallback:** [placeholder.ts:10](packages/web/src/lib/placeholder.ts#L10) — `'#6B4A2B'` gradient stop. **Change:** 3-line swap across 3 files; no DB / CMS schema change. **Verify:** [brand-guidelines.pdf](../assets/brand-guidelines.pdf) reference + screenshot any high-contrast surface (Footer reskin / q7 target, eyebrow text, primary CTA) before/after. | Swapped 3 source files; web rebuilt + restarted, new token `#533E27` confirmed in served CSS (`/assets/index-D3qZrXoG.css`, served `--accent-deep: #533E27`). CMS admin theme edit committed but **won't render in /admin until `pnpm --filter @cosmedic/cms build` succeeds** — currently blocked by a pre-existing TS path-alias error (`web/src/content/seed.ts` uses `@/lib/cms-proxy` which CMS's tsconfig can't resolve). Tracked as separate fix-up; running CMS process still serves 200 from old in-memory build. | `a5e5e9e` |
