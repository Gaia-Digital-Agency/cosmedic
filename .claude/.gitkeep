# Keep this directory in git.
#
# This folder is for project-local Claude Code settings (e.g. .claude/settings.json
# for permissions, hooks). When settings.json exists, this .gitkeep becomes redundant
# but is harmless to keep.
#
# Worktrees created by Claude under .claude/worktrees/ are intentionally gitignored.
